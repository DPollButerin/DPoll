import { createContext } from "react";

/*
@dev ConnectionContext is a React Context that will be used to share the state of the connection between components
*/
const ConnectionContext = createContext();

export default ConnectionContext;
