import { createContext } from "react";

/*
@dev ContractsContext is a React Context that will be used to share the state of the contracts between components
*/
const ContractsContext = createContext();

export default ContractsContext;
